service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read;
      allow write: if request.auth.uid == uid;
    }
    match /works/{uid} {
      allow get: if request.auth.uid == uid || !exists(request.path) || resource.data.privacy == 'public' || resource.data.privacy == 'limited';
      allow list: if request.auth.uid == uid || resource.data.privacy == 'public';
      allow write: if request.auth.uid == uid;
    }
  }
}
